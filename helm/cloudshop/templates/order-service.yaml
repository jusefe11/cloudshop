apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: cloudshop
spec:
  type: {{ .Values.order.service.type }}
  selector:
    app: order-service
  ports:
    - protocol: TCP
      port: {{ .Values.order.service.port }}
      targetPort: {{ .Values.order.service.port }}
